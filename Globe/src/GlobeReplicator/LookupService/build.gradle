application {
    mainClassName = 'com.github.arucard21.globe.replicator.lookupservice.LSApplication'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'com.github.arucard21.globe.replicator.lookupservice.LSApplication'
        )
    }
}